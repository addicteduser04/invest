{
  "name": "@bvc/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "dev": "tsx watch src/index.ts",
    "market:daily": "tsx src/daily-ingestion-cli.ts"
  },
  "dependencies": {
    "@bvc/market-data": "workspace:*",
    "@bvc/market-ingestion": "workspace:*",
    "@bvc/portfolio-engine": "workspace:*",
    "pg": "^8.16.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.15.0",
    "tsx": "^4.20.0",
    "vitest": "^3.2.0"
  }
}
